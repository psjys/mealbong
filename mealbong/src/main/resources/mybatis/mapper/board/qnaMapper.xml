<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 위 코드가 mapper 설정을 하는 코드 -->

<!-- Mapper를 사용하는 Repository의 전체 경로를 적어줘야함. -->
<mapper namespace="com.project.mealbong.board.QnaMapper">
    <select id="qnaList" resultType="com.project.mealbong.board.QnaDTO">
        select qna_num, qna_title, qna_content, qna_date, qna_file, ans_content, ans_date from qna
        order by qna_num desc;
    </select>

    <select id="detail" resultType="com.project.mealbong.board.QnaDTO">
        select * from qna
        where qna_num = #{qna_num}
    </select>

    <insert id="insert">
        insert into qna(user_id, qna_title, qna_content, qna_file) values (
                "hsy", #{qna_title}, #{qna_content}, #{qna_file}
        )
    </insert>

    <update id="update">
        update qna set qna_title=#{qna_title}, qna_content=#{qna_content}, qna_date=now(), qna_file=#{qna_file}
        where qna_num=#{qna_num}
    </update>

    <delete id="delete">
        delete from qna
        where qna_num=#{qna_num}
    </delete>

</mapper>